# تغییرات استخراج‌شده از گفت‌وگوی پیوست

## تصاویر ۲ و ۳

در هیرو، پس‌زمینه مرجع سی‌فایو باید به‌صورت لایه‌ای کم‌رنگ و با کانتورهای آزاد پشت محتوا دیده شود؛ صرفاً زمینه آبی روشن کافی نیست. تیتر اصلی باید کوچکتر شود تا در دسکتاپ به دو خط کنترل‌شده برسد، در حالی‌که متن توضیحی زیر آن کمی بزرگتر و خواناتر می‌شود. عمق حرکت تصویر هیرو در hover نیز افزایش می‌یابد.

در سکشن اتصال ابزارها، تیتر باید کوچکتر و متن توضیحی بزرگتر شود. برای تصویرِ سمت چپ نیز باید ترکیب تصویر به سمت مرکز منتقل یا قاب‌بندی شود تا فضای خالیِ سمت چپ تصویر به چشم نیاید.

## تصاویر ۴ و ۵

بخش «مراحل طراحی سایت با آسمان» باید از ردیف ساده فعلی به چیدمان مرجع سی‌فایو تبدیل شود: یک قاب عمودی میانی با نسبت ویدئو (فعلاً با تصویر جایگزین)، دو مرحله در راست و دو مرحله در چپ، و نشان‌های شماره‌دار دایره‌ای متصل به مسیر افقی. تیتر و توضیح بالای سکشن حفظ می‌شوند، اما مقیاس و تراکم کلی آن به نمونه مرجع نزدیک‌تر خواهد شد.

## تصاویر ۶ و ۷

در سکشن مدیریت، تیتر باید کوچکتر و متن اصلی خواناتر شود؛ تصویر دستگاه‌ها باید در قاب خود جلوتر و متراکم‌تر دیده شود تا فضای خالی آزاردهنده نداشته باشد. CTA روی زمینه آبی نیز باید با کنتراست قطعی و دیده‌شدن فوری بازطراحی شود.

برای جلوگیری از یکدستی چند سکشن آبی پیاپی، پالت از یک رنگ مکمل گرم و کنترل‌شده در جزئیات و نواحی تأکیدی استفاده می‌کند؛ زمینه اصلی برند همچنان آبی می‌ماند. کارت‌های امکانات باید از زمینه جدا باشند، در hover سایه و رنگ تأکیدی اختصاصی بگیرند و متن‌های کوتاه زیر عنوان‌ها با اندازه بزرگتر دیده شوند.

## تصویر ۸

CTA میانی فعلی به علت آمیختگی بیش از حد چند لایه آبی، تصویر کم‌اثر و سلسله‌مراتب نامناسب باید به‌طور کامل بازطراحی شود. نسخه جدید از یک تصویر رشد سه‌بعدی متراکم، متن با مقیاس متعادل، CTA سفید با کنتراست بالا و تأکیدهای بسیار محدود کهربایی برای شکستن یکنواختی آبی استفاده می‌کند. برای قاب میانی مراحل نیز یک تصویر عمودی مستقل، مناسب جایگزینی ویدئوی مرجع، تهیه می‌شود.

## پالت تکمیلی تأییدشده

پایه تیره از سرمه‌ای و آبی پروسی `#023047`، آبی سبز `#219EBC` و آبی روشن `#8ECAE6` تشکیل می‌شود. زرد انتخابی `#FFB703` و نارنجی نارنگی `#FB8500` تنها برای تأکیدهای کوچک، شماره‌ها، هاله‌ها و حالات hover استفاده می‌شوند تا هویت آبی آسمان غالب باقی بماند. خاکستری نوشتاری `#64748B` و سطح روشن `#E2E8F0` نیز نقش تعادل و خوانایی را دارند.
